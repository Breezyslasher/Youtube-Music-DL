pytest>=8
# starlette's TestClient needs an httpx client library; recent starlette
# (>=1.x) imports httpx2, older imports httpx. Install both so the tests
# run against either.
httpx
httpx2
# The page-load test drives a real browser. Optional: without playwright,
# or without a chromium binary to point it at, those tests skip - the
# rest of the suite does not need it.
playwright
